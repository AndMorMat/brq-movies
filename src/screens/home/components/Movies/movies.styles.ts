import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  listContainer: {
    paddingTop: 15,
  },
  image: {
    margin: 15,
    borderRadius: 8
  },
  loader: {
    marginTop: 100,
    color: '#EC8B00'
  }
})